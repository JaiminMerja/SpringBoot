package com;

import org.springframework.stereotype.Service;
import org.springframework.web.multipart.MultipartFile;

import java.io.File;
import java.io.IOException;
import java.nio.file.*;

@Service
public class FileStorageService {

    // Specify the directory where files will be stored
    private final Path uploadDir = Paths.get("uploads").toAbsolutePath();  // Absolute path of the 'uploads' directory

    // Method to store the file (image)
    public String storeFile(MultipartFile file) {
        try {
            // Ensure the upload directory exists
            if (!Files.exists(uploadDir)) {
                Files.createDirectories(uploadDir);  // Create directory if it doesn't exist
                System.out.println("Created upload directory at: " + uploadDir.toAbsolutePath());
            }

            // Log if the directory exists
            if (Files.exists(uploadDir)) {
                System.out.println("Upload directory exists at: " + uploadDir.toAbsolutePath());
            } else {
                System.out.println("Failed to create or locate upload directory.");
            }

            // Get the original file name
            String fileName = file.getOriginalFilename();
